{
  "name": "pmp-trainer-cloud",
  "version": "2.0.0",
  "description": "PMP Exam Trainer with cloud sync via Supabase",
  "private": true,
  "scripts": {
    "dev": "npx serve public -l 3000",
    "build": "echo 'Static site - no build needed'"
  }
}
